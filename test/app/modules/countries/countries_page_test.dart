import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:covid_tracker_app/app/modules/countries/countries_page.dart';

main() {
  testWidgets('CountriesPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CountriesPage(title: 'Countries')));
    //  final titleFinder = find.text('Countries');
    //  expect(titleFinder, findsOneWidget);
  });
}
